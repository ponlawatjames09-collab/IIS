sed -i 's/setFetchError(`Could not retrieve records from tab ${activeMonthYear}. Verify that tab exists inside your Linked Spreadsheet.`);/if (err.message \&\& err.message.includes("403")) { setAuthError("Failed to fetch Google Sheets. This usually happens because the Google Sheets API is not enabled in your Google Cloud (Firebase) project, or the spreadsheet is not shared with your account.\\n\\nPlease go to Google Cloud Console, select your Firebase project, and Enable the Google Sheets API."); } else { setFetchError(`Could not retrieve records from tab ${activeMonthYear}. Verify that tab exists inside your Linked Spreadsheet.`); }/g' src/App.tsx
