sed -i 's/console.error("Failed to load yearly jobs", err);/console.error("Failed to load yearly jobs", err);\n      if (err.message \&\& err.message.includes("403")) {\n        setAuthError("Failed to fetch Google Sheets. This usually happens because the Google Sheets API is not enabled in your Google Cloud (Firebase) project, or the spreadsheet is not shared with your account.\\n\\nPlease go to Google Cloud Console, select your Firebase project, and Enable the Google Sheets API.");\n      }/g' src/App.tsx
